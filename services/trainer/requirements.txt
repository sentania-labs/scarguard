# Torch/torchvision/CUDA are supplied together by the digest-pinned L4T base.
# Do not pip-upgrade them independently: the Jetson ABI is part of the image.
numpy==1.26.4
ultralytics==8.4.56
opencv-python-headless==4.10.0.84
# roboflow and pillow-heif move together. pillow-heif 1.3.0 is the first
# release clearing GHSA-5gjj-6r7v-ph3x, and 1.x has no register_avif_opener,
# which roboflow 1.1.61 called on import. roboflow 1.2.0 gets AVIF from
# pillow-avif-plugin instead, so it imports cleanly against pillow-heif 1.x.
# This supersedes the pillow-heif==0.22.0 pin from #179.
roboflow==1.2.0
pillow-heif==1.3.0
redis==5.2.1
pyyaml==6.0.2
requests==2.33.0
